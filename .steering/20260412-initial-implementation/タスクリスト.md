# 初回実装 タスクリスト

## 凡例

- `[ ]` 未着手
- `[x]` 完了
- `[-]` スキップ / 対象外

---

## セクション 1: プロジェクト初期化

> **学習テーマ**: Next.js App Router の構造と設定ファイルの役割

- [ ] 1-1. `create-next-app` でプロジェクト作成
  - TypeScript: Yes
  - ESLint: Yes
  - Tailwind CSS: Yes
  - `src/` ディレクトリ: Yes
  - App Router: Yes
  - import alias: `@/*`
- [ ] 1-2. デフォルトのサンプルコード・スタイルを削除してクリーンな状態にする
- [ ] 1-3. 必要パッケージをインストール
  - `prisma` `@prisma/client`
  - `next-auth@beta` `@auth/prisma-adapter`
  - `zod`
  - `react-hook-form` `@hookform/resolvers`
  - `recharts`
  - `vitest` `@vitejs/plugin-react` `@testing-library/react`（開発依存）
- [ ] 1-4. `prisma init` で Prisma を初期化
- [ ] 1-5. `package.json` にスクリプトを追加
  - `"type-check": "tsc --noEmit"`
  - `"test": "vitest"`
- [ ] 1-6. リポジトリ構造定義書に従ってフォルダ構造を作成
  - `src/components/layout/`
  - `src/components/weight/`
  - `src/components/meal/`
  - `src/components/exercise/`
  - `src/components/dashboard/`
  - `src/lib/`
- [ ] 1-7. `npm run dev` で起動確認

---

## セクション 2: DB設計・Prisma セットアップ

> **学習テーマ**: ORM・マイグレーション・DB接続の仕組みを理解する

- [ ] 2-1. Supabase でプロジェクトを作成する
- [ ] 2-2. Supabase から `DATABASE_URL`（接続文字列）を取得する
- [ ] 2-3. `.env.local` を作成して `DATABASE_URL` を設定する
- [ ] 2-4. `prisma/schema.prisma` に以下のモデルを定義する
  - `User`
  - `Account`（NextAuth.js 必須）
  - `Session`（NextAuth.js 必須）
  - `WeightRecord`（`@@unique([userId, date])`）
  - `WeightGoal`
  - `MealRecord`
  - `ExerciseRecord`
- [ ] 2-5. `npx prisma migrate dev --name init` でマイグレーション実行
- [ ] 2-6. `npx prisma studio` でテーブルが作成されたことを確認
- [ ] 2-7. `src/lib/db.ts` に Prisma Client のシングルトンを作成する

---

## セクション 3: 認証（NextAuth.js）

> **学習テーマ**: OAuth フロー・セッション管理・ミドルウェアによるルート保護

- [ ] 3-1. Google Cloud Console で OAuth 2.0 クライアントを作成する
  - 承認済みリダイレクトURI: `http://localhost:3000/api/auth/callback/google`
- [ ] 3-2. `.env.local` に以下を追加する
  - `AUTH_SECRET`（`npx auth secret` で生成）
  - `AUTH_GOOGLE_ID`
  - `AUTH_GOOGLE_SECRET`
- [ ] 3-3. `src/lib/auth.ts` に NextAuth.js の設定を記述する
  - Google Provider
  - Prisma Adapter
- [ ] 3-4. `src/app/api/auth/[...nextauth]/route.ts` にハンドラを配置する
- [ ] 3-5. `middleware.ts` を作成してログイン必須ルートを保護する
  - 保護対象: `/dashboard`、`/weight`、`/meal`、`/exercise`
  - 未認証の場合は `/`（ログインページ）へリダイレクト
- [ ] 3-6. `src/app/page.tsx` にログインページを実装する
  - 「Googleでログイン」ボタン
  - デザインシステムに沿ったスタイリング
- [ ] 3-7. Google アカウントでログイン・ログアウトできることを確認する
- [ ] 3-8. 未ログイン状態で `/dashboard` にアクセスするとリダイレクトされることを確認する

---

## セクション 4: レイアウト・ナビゲーション

> **学習テーマ**: Server/Client Component の違いと使い分け・共通UIの構築

- [ ] 4-1. `src/app/layout.tsx` にグローバルレイアウトを実装する
  - 背景エフェクト（グローエフェクト）
  - `機能設計書.md` のデザインシステムに沿った背景色（`bg-gray-950`）
- [ ] 4-2. `src/components/layout/Header.tsx` を実装する（Client Component）
  - アプリタイトル
  - ログアウトボタン
- [ ] 4-3. `src/components/layout/BottomNav.tsx` を実装する（Client Component）
  - ダッシュボード / 体重 / 食事 / 運動 の4タブ
  - アクティブなタブはグラデーションアイコン
- [ ] 4-4. `src/app/(authenticated)/layout.tsx` を作成する
  - Header + BottomNav を含む認証済みページ共通レイアウト
- [ ] 4-5. `src/app/(authenticated)/dashboard/page.tsx` にプレースホルダーを配置して表示確認する

---

## セクション 5: 体重記録機能

> **学習テーマ**: Server Actions・API Route の設計・Recharts グラフの実装

- [ ] 5-1. `src/app/api/weight/route.ts` を作成する
  - `GET`: 直近30件の体重記録を返す
  - `POST`: 体重を登録する（同日は上書き）
- [ ] 5-2. `src/app/api/weight/[id]/route.ts` を作成する
  - `DELETE`: 指定IDの記録を削除する（他ユーザーの記録は403）
- [ ] 5-3. `src/app/api/weight/goal/route.ts` を作成する
  - `GET`: 目標体重を取得する
  - `PUT`: 目標体重を登録・更新する
- [ ] 5-4. `src/lib/validations.ts` に Zod スキーマを定義する（体重・目標体重）
- [ ] 5-5. `src/components/weight/WeightForm.tsx` を実装する
  - 日付・体重（kg）の入力フォーム
  - 保存後にフォームをリセット
- [ ] 5-6. `src/components/weight/WeightGoalForm.tsx` を実装する
  - 目標体重の入力フォーム
- [ ] 5-7. `src/components/weight/WeightChart.tsx` を実装する（Client Component）
  - Recharts の `LineChart` で過去30日の推移を折れ線グラフ表示
  - 目標体重を水平の基準線（`ReferenceLine`）で表示
- [ ] 5-8. `src/components/weight/WeightHistory.tsx` を実装する
  - 記録の一覧表示
  - 各行に削除ボタン
- [ ] 5-9. `src/app/(authenticated)/weight/page.tsx` でコンポーネントを組み合わせる
- [ ] 5-10. 体重の登録・削除・グラフ表示・目標設定が正常に動作することを確認する

---

## セクション 6: 食事記録機能

> **学習テーマ**: タブUIの状態管理・フォーム設計・カロリー集計

- [ ] 6-1. `src/app/api/meal/route.ts` を作成する
  - `GET`: 指定日の食事記録を返す（クエリ `?date=YYYY-MM-DD`）
  - `POST`: 食事を登録する
- [ ] 6-2. `src/app/api/meal/[id]/route.ts` を作成する
  - `DELETE`: 指定IDの記録を削除する（他ユーザーの記録は403）
- [ ] 6-3. `src/lib/validations.ts` に Zod スキーマを追加する（食事記録）
- [ ] 6-4. `src/components/meal/MealTabBar.tsx` を実装する（Client Component）
  - 朝食 / 昼食 / 夕食 / 間食 のタブ切り替え
- [ ] 6-5. `src/components/meal/MealForm.tsx` を実装する
  - 食品名・カロリーの入力フォーム
  - 保存後にフォームをリセット
- [ ] 6-6. `src/components/meal/MealList.tsx` を実装する
  - 選択中の食事区分の記録一覧 + 削除ボタン
- [ ] 6-7. `src/app/(authenticated)/meal/page.tsx` でコンポーネントを組み合わせる
  - 合計摂取カロリーを表示
- [ ] 6-8. 食事の登録・削除・タブ切り替え・合計カロリー表示が正常に動作することを確認する

---

## セクション 7: 運動記録機能

> **学習テーマ**: セクション5・6のパターンを応用して自走する

- [ ] 7-1. `src/app/api/exercise/route.ts` を作成する
  - `GET`: 指定日の運動記録を返す（クエリ `?date=YYYY-MM-DD`）
  - `POST`: 運動を登録する
- [ ] 7-2. `src/app/api/exercise/[id]/route.ts` を作成する
  - `DELETE`: 指定IDの記録を削除する（他ユーザーの記録は403）
- [ ] 7-3. `src/lib/validations.ts` に Zod スキーマを追加する（運動記録）
- [ ] 7-4. `src/components/exercise/ExerciseForm.tsx` を実装する
  - 種目・時間（分）・消費カロリーの入力フォーム
- [ ] 7-5. `src/components/exercise/ExerciseList.tsx` を実装する
  - 記録一覧 + 削除ボタン
- [ ] 7-6. `src/app/(authenticated)/exercise/page.tsx` でコンポーネントを組み合わせる
  - 合計消費カロリーを表示
- [ ] 7-7. 運動の登録・削除・合計カロリー表示が正常に動作することを確認する

---

## セクション 8: ダッシュボード

> **学習テーマ**: 複数データの並行取得・カロリー収支計算・revalidatePath

- [ ] 8-1. `src/components/dashboard/WeightSummary.tsx` を実装する
  - 今日の体重を表示（未記録の場合は「未記録」）
  - 目標体重との差分を表示
- [ ] 8-2. `src/components/dashboard/CalorieSummary.tsx` を実装する
  - 摂取カロリー / 消費カロリー / 収支を表示
  - グラデーションプログレスバーで収支を可視化
- [ ] 8-3. `src/app/(authenticated)/dashboard/page.tsx` を実装する
  - `Promise.all` で体重・食事・運動データを並行取得
  - 各サマリーコンポーネントへデータを渡す
  - 各記録ページへのナビゲーションボタン
- [ ] 8-4. ダッシュボードに今日の体重・カロリー収支が正しく表示されることを確認する

---

## セクション 9: 仕上げ・品質チェック

> **学習テーマ**: CI/CD 設定・型安全性の確認・レスポンシブ対応

- [ ] 9-1. `npm run lint` でエラーがないことを確認し、修正する
- [ ] 9-2. `npx tsc --noEmit` で型エラーがないことを確認し、修正する
- [ ] 9-3. `npm run build` でビルドが成功することを確認する
- [ ] 9-4. `.github/workflows/ci.yml` を作成する
  - Lint・型チェック・ビルドのジョブを設定
- [ ] 9-5. スマホブラウザ（または DevTools のモバイルビュー）でレスポンシブ表示を確認する
  - 画面幅 375px（iPhone SE）での表示崩れがないことを確認
- [ ] 9-6. 全機能の E2E 動作確認を行う
  - [ ] Googleログイン・ログアウト
  - [ ] 体重の登録・削除・グラフ確認
  - [ ] 食事の登録・削除・タブ切り替え
  - [ ] 運動の登録・削除
  - [ ] ダッシュボードでの収支確認
- [ ] 9-7. `.env.local` の変数名を `.env.local.example` にまとめてコミットする（値は除く）

---

## 進捗サマリー

| セクション | 状態 |
|---|---|
| 1. プロジェクト初期化 | 未着手 |
| 2. DB設計・Prisma セットアップ | 未着手 |
| 3. 認証（NextAuth.js） | 未着手 |
| 4. レイアウト・ナビゲーション | 未着手 |
| 5. 体重記録機能 | 未着手 |
| 6. 食事記録機能 | 未着手 |
| 7. 運動記録機能 | 未着手 |
| 8. ダッシュボード | 未着手 |
| 9. 仕上げ・品質チェック | 未着手 |
